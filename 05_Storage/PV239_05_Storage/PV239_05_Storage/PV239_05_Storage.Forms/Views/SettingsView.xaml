<?xml version="1.0" encoding="utf-8" ?>
<views:ViewBase xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:resources="clr-namespace:PV239_05_Storage.Core.Resources;assembly=PV239_05_Storage.Core"
                xmlns:views="clr-namespace:PV239_05_Storage.Forms.Views"
                xmlns:viewModels="clr-namespace:PV239_05_Storage.Core.ViewModels;assembly=PV239_05_Storage.Core"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                mc:Ignorable="d"
                x:Class="PV239_05_Storage.Forms.Views.SettingsView"
                d:DataContext="{d:DesignInstance viewModels:SettingsViewModel}"
                Title="{x:Static resources:SettingsViewResources.Page_Title}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0"
               Grid.Column="0"
               HorizontalOptions="Start"
               Text="{x:Static resources:SettingsViewResources.Username_Label_Text}"/>
        <Entry Grid.Row="0"
               Grid.Column="1"
               Text="{Binding Username}"/>

        <Label Grid.Row="1"
               Grid.Column="0"
               HorizontalOptions="Start"
               Text="{x:Static resources:SettingsViewResources.Language_Label_Text}"/>
        <Entry Grid.Row="1"
               Grid.Column="1"
               Text="{Binding Language}"/>

        <Button Grid.Row="2"
                Grid.Column="0"
                HorizontalOptions="Start"
                VerticalOptions="End"
                Text="{x:Static resources:SettingsViewResources.Cancel_Button_Text}"
                Style="{StaticResource CancelButtonStyle}"
                Command="{Binding CancelCommand}"/>

        <Button Grid.Row="2"
                Grid.Column="1"
                HorizontalOptions="EndAndExpand"
                VerticalOptions="End"
                Text="{x:Static resources:SettingsViewResources.Save_Button_Text}"
                Style="{StaticResource PrimaryButtonStyle}"
                Command="{Binding SaveCommand}"/>
    </Grid>
</views:ViewBase>