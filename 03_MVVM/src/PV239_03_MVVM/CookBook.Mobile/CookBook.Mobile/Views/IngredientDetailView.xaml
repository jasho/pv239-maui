<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:CookBook.Mobile.ViewModels;assembly=CookBook.Mobile"
             x:Class="CookBook.Mobile.Views.IngredientDetailView"
             x:DataType="viewModels:IngredientDetailViewModel">
    <Grid RowDefinitions="300, *, 80">
        <Label Grid.Row="0"
               FontSize="30"
               Text="{Binding Data.Name}"/>

        <ScrollView Grid.Row="1">
            <Label HorizontalOptions="Start" VerticalOptions="Start"
                   FontSize="25"
                   Text="{Binding Data.Description}"
                   BackgroundColor="Transparent" />
        </ScrollView>

        <Grid Grid.Row="2"
              ColumnDefinitions="*, *"
              Padding="10"
              ColumnSpacing="10">
            <Button Grid.Column="0"
                    Text="Delete"
                    Style="{StaticResource ErrorButtonStyle}" />

            <Button Grid.Column="1"
                    Text="Edit"
                    Command="{Binding EditCommand}"
                    CommandParameter="{Binding Data.Id}"
                    Style="{StaticResource PrimaryButtonStyle}" />
        </Grid>
    </Grid>
</ContentPage>